# carvera-control todo

- fix xmodem cancel cleanup
- dialog for editing config items
- animate file as it runs (using status stream until Run becomes Idle)
- use xmodem file load / cancel (fast) instead of md5sum (slow)
